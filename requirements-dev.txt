# Typing / IDE: stub bpy for editors and `python -m py_compile` outside Blender.
# Use with: py -3.11 -m venv .venv && .\.venv\Scripts\activate && pip install -r requirements-dev.txt
fake-bpy-module-4.5
